<br>
<form method="post" action="{{ path('admin_rezervation_delete', {'id': rezervation.id}) }}" onsubmit="return confirm('Bu içeriği silmek istediğinize emin misiniz?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rezervation.id) }}">
    <button class="btn btn-block btn-danger ">Sil</button>
</form>
